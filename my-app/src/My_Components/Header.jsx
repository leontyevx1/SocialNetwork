import React from 'react';
import logo from './logo/logo.png'

const Header = () => {
    return (
        <header className='header'>
            <img src={logo}/>
        </header>
    );
};

export default Header;